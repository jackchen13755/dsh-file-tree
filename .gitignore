node_modules/
*.log
.DS_Store

# The vendored code-server (scripts/install-code-server.sh): ~640MB unpacked,
# shipped inside the packed plugin (package.json#files) so nobody downloads it
# again, but far too large to track in git.
vendor/
.code-server
